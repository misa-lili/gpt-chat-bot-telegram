{
  "name": "gpt-chat-bot-telegram",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "nodemon",
    "build": "npx tsc",
    "service": "sudo cp ./gpt-chat-bot-telegram.service /etc/systemd/system/gpt-chat-bot-telegram.service && sudo systemctl daemon-reload && sudo systemctl enable gpt-chat-bot-telegram.service",
    "update": "git pull && sudo systemctl restart gpt-chat-bot-telegram.service && journalctl -f -n 1000 -u gpt-chat-bot-telegram.service"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "node-telegram-bot-api": "^0.61.0",
    "openai": "^3.3.0"
  },
  "devDependencies": {
    "@types/node-telegram-bot-api": "^0.61.6",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  }
}
